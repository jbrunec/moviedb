import Filter from "./Filter";
import FilterContainer from "./FilterContainer";

const FilterSection = () => {
  return (
    <FilterContainer>
      <Filter />
    </FilterContainer>
  );
};

export default FilterSection;
